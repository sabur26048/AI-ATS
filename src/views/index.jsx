import React from "react";

function Welcome({ name }) {
  return (
    <html>
      <head>
        <title>React as Template</title>
      </head>
      <body>
        <h1>Hello, {name}!</h1>
      </body>
    </html>
  );
}

export default Welcome;
